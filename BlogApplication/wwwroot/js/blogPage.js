
const btn = document.getElementById("sortByDate");


$(document).ready(() => {
    const $sortButton = $("#sortByDate");
    $sortButton.css("margin-bottom", "5%");
    $sortButton.on("click", () => {
       
    });
});